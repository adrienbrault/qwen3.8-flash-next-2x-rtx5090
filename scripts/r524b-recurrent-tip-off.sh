#!/usr/bin/env bash
# R524b — OFF arm of R524: the daily image (no tip overlay), same echo-mode agent replay, fingerprints and fn_bench code c1/c4.
# RUN (queued): popped by r515-queue-chain from flashnext-queue.txt.
TAG=OFF exec bash /srv/qwen5090/r524-recurrent-tip.sh
